{% extends 'base.html.twig' %}

{% block body %}
    <h1>Refplace</h1>

    <table>
        <tbody>
            <tr>
                <th>Label</th>
                <td>{{ refPlace.label }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ refPlace.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('refplace_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('refplace_edit', { 'id': refPlace.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
